# Argon RTC Configuration
# minute hour day-of-month month day-of-week on|off
# The legacy-compatible parser supports only * and comma-separated integers.
# The month field is preserved but intentionally ignored.
